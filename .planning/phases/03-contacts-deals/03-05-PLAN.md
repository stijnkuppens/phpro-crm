---
phase: 03-contacts-deals
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/activities/types.ts
  - src/features/activities/queries/get-activities.ts
  - src/features/tasks/types.ts
  - src/features/tasks/queries/get-tasks.ts
  - src/features/communications/types.ts
  - src/features/communications/queries/get-communications.ts
  - src/features/deals/components/deal-detail.tsx
  - src/app/admin/deals/[id]/page.tsx
autonomous: true
requirements: [DEAL-04]

must_haves:
  truths:
    - "Deal detail page shows tabs for activities, tasks, and communications linked to that deal"
    - "Activities tab shows only activities with this deal's deal_id"
    - "Tasks tab shows only tasks with this deal's deal_id"
    - "Communications tab shows only communications with this deal's deal_id"
    - "All data is server-fetched and passed as initialData (no client-side waterfall)"
  artifacts:
    - path: "src/features/activities/types.ts"
      provides: "ActivityFilters with deal_id field"
      contains: "deal_id"
    - path: "src/features/tasks/types.ts"
      provides: "TaskFilters with deal_id field"
      contains: "deal_id"
    - path: "src/features/communications/types.ts"
      provides: "CommunicationFilters with deal_id field"
      contains: "deal_id"
    - path: "src/features/deals/components/deal-detail.tsx"
      provides: "Deal detail with activities/tasks/communications tabs"
      contains: "Activiteiten"
  key_links:
    - from: "src/app/admin/deals/[id]/page.tsx"
      to: "getActivities, getTasks, getCommunications"
      via: "parallel fetch with deal_id filter"
      pattern: "Promise\\.all"
    - from: "src/features/deals/components/deal-detail.tsx"
      to: "activities/tasks/communications data"
      via: "props from server component"
      pattern: "initialActivities|activities"
---

<objective>
Add deal_id filter support to activities, tasks, and communications queries, then show linked data on the deal detail page in tabs.

Purpose: Deal detail page must show all related activities, tasks, and communications for that deal (DEAL-04).
Output: Updated queries with deal_id filter, enhanced deal detail page with tabs.
</objective>

<execution_context>
@/Users/stijnkuppens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijnkuppens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts -->

From src/features/activities/types.ts:
```typescript
export type ActivityFilters = {
  search?: string; type?: string; account_id?: string; is_done?: boolean;
};
// MISSING: deal_id
```

From src/features/activities/queries/get-activities.ts:
```typescript
export const getActivities = cache(async ({ filters, page, pageSize }) => {
  // Has filters for search, type, account_id, is_done
  // MISSING: deal_id filter
});
```

From src/features/tasks/types.ts:
```typescript
export type TaskFilters = {
  status?: string; priority?: string; assigned_to?: string;
};
// MISSING: deal_id
```

From src/features/tasks/queries/get-tasks.ts:
```typescript
// Has filters for status, priority, assigned_to
// MISSING: deal_id filter
```

From src/features/communications/types.ts:
```typescript
export type CommunicationFilters = {
  account_id?: string; type?: string; contact_id?: string;
};
// MISSING: deal_id
```

From src/features/communications/queries/get-communications.ts:
```typescript
// Has filters for account_id, type, contact_id
// MISSING: deal_id filter
```

From src/features/deals/components/deal-detail.tsx:
```typescript
type Props = { deal: DealWithRelations; };
// Currently shows Deal Info and Contact cards
// No tabs for linked data
```

From src/app/admin/deals/[id]/page.tsx:
```typescript
// Currently only fetches deal via getDeal(id)
// Does not fetch activities, tasks, communications
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deal_id filter to activities, tasks, and communications queries</name>
  <files>src/features/activities/types.ts, src/features/activities/queries/get-activities.ts, src/features/tasks/types.ts, src/features/tasks/queries/get-tasks.ts, src/features/communications/types.ts, src/features/communications/queries/get-communications.ts</files>
  <read_first>
    src/features/activities/types.ts
    src/features/activities/queries/get-activities.ts
    src/features/tasks/types.ts
    src/features/tasks/queries/get-tasks.ts
    src/features/communications/types.ts
    src/features/communications/queries/get-communications.ts
  </read_first>
  <action>
**1. `src/features/activities/types.ts`** — add `deal_id` to ActivityFilters:
```typescript
export type ActivityFilters = {
  search?: string;
  type?: string;
  account_id?: string;
  is_done?: boolean;
  deal_id?: string;  // ADD
};
```

**2. `src/features/activities/queries/get-activities.ts`** — add deal_id filter after the existing `is_done` filter block (around line 42):
```typescript
if (filters?.deal_id) {
  query = query.eq('deal_id', filters.deal_id);
}
```

**3. `src/features/tasks/types.ts`** — add `deal_id` to TaskFilters:
```typescript
export type TaskFilters = {
  status?: string;
  priority?: string;
  assigned_to?: string;
  deal_id?: string;  // ADD
};
```

**4. `src/features/tasks/queries/get-tasks.ts`** — add deal_id filter after the existing `assigned_to` filter block (around line 40):
```typescript
if (filters?.deal_id) {
  query = query.eq('deal_id', filters.deal_id);
}
```

**5. `src/features/communications/types.ts`** — add `deal_id` to CommunicationFilters:
```typescript
export type CommunicationFilters = {
  account_id?: string;
  type?: string;
  contact_id?: string;
  deal_id?: string;  // ADD
};
```

**6. `src/features/communications/queries/get-communications.ts`** — add deal_id filter after the existing `contact_id` filter block (around line 40):
```typescript
if (filters?.deal_id) {
  query = query.eq('deal_id', filters.deal_id);
}
```

All changes are additive — one line added to each type, one filter block added to each query.
  </action>
  <verify>
    <automated>cd /Users/stijnkuppens/phpro/ai-projects/phpro_crm && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/activities/types.ts contains `deal_id?: string`
    - src/features/activities/queries/get-activities.ts contains `filters?.deal_id`
    - src/features/tasks/types.ts contains `deal_id?: string`
    - src/features/tasks/queries/get-tasks.ts contains `filters?.deal_id`
    - src/features/communications/types.ts contains `deal_id?: string`
    - src/features/communications/queries/get-communications.ts contains `filters?.deal_id`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three query modules (activities, tasks, communications) support deal_id filtering; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add linked data tabs to deal detail page</name>
  <files>src/features/deals/components/deal-detail.tsx, src/app/admin/deals/[id]/page.tsx</files>
  <read_first>
    src/features/deals/components/deal-detail.tsx
    src/app/admin/deals/[id]/page.tsx
    src/features/deals/queries/get-deal.ts
    src/features/activities/queries/get-activities.ts
    src/features/tasks/queries/get-tasks.ts
    src/features/communications/queries/get-communications.ts
    src/features/activities/types.ts
    src/features/tasks/types.ts
    src/features/communications/types.ts
  </read_first>
  <action>
**1. Update `src/app/admin/deals/[id]/page.tsx`:**

Add parallel fetch for activities, tasks, and communications filtered by deal_id:
```typescript
import { getActivities } from '@/features/activities/queries/get-activities';
import { getTasks } from '@/features/tasks/queries/get-tasks';
import { getCommunications } from '@/features/communications/queries/get-communications';

export default async function DealDetailPage({ params }: Props) {
  const { id } = await params;

  const [deal, activitiesResult, tasksResult, communicationsResult] = await Promise.all([
    getDeal(id),
    getActivities({ filters: { deal_id: id }, pageSize: 50 }),
    getTasks({ filters: { deal_id: id }, pageSize: 50 }),
    getCommunications({ filters: { deal_id: id }, pageSize: 50 }),
  ]);

  if (!deal) notFound();

  return (
    <div className="space-y-6">
      <PageHeader title={deal.title} breadcrumbs={[...]} />
      <DealDetail
        deal={deal}
        activities={activitiesResult.data}
        tasks={tasksResult.data}
        communications={communicationsResult.data}
      />
    </div>
  );
}
```

**2. Update `src/features/deals/components/deal-detail.tsx`:**

This is a server component (no `'use client'` directive currently). Add linked data display using Tabs.

a. Update Props:
```typescript
import type { ActivityWithRelations } from '@/features/activities/types';
import type { TaskWithRelations } from '@/features/tasks/types';
import type { CommunicationWithDetails } from '@/features/communications/types';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

type Props = {
  deal: DealWithRelations;
  activities: ActivityWithRelations[];
  tasks: TaskWithRelations[];
  communications: CommunicationWithDetails[];
};
```

b. After the existing cards (Deal Info, Contact & Eigenaar, Description, Afsluiting), add a linked data section spanning full width:

```tsx
<div className="lg:col-span-2">
  <Tabs defaultValue="activities">
    <TabsList>
      <TabsTrigger value="activities">Activiteiten ({activities.length})</TabsTrigger>
      <TabsTrigger value="tasks">Taken ({tasks.length})</TabsTrigger>
      <TabsTrigger value="communications">Communicatie ({communications.length})</TabsTrigger>
    </TabsList>

    <TabsContent value="activities">
      {activities.length === 0 ? (
        <p className="text-sm text-muted-foreground py-4">Geen activiteiten gekoppeld aan deze deal.</p>
      ) : (
        <div className="space-y-2 mt-2">
          {activities.map(a => (
            <Card key={a.id}>
              <CardContent className="py-3 px-4">
                <div className="flex items-center justify-between">
                  <div>
                    <span className="font-medium text-sm">{a.subject}</span>
                    <span className="text-xs text-muted-foreground ml-2">{a.type}</span>
                  </div>
                  <span className="text-xs text-muted-foreground">
                    {new Date(a.date).toLocaleDateString('nl-BE')}
                  </span>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>
      )}
    </TabsContent>

    <TabsContent value="tasks">
      {tasks.length === 0 ? (
        <p className="text-sm text-muted-foreground py-4">Geen taken gekoppeld aan deze deal.</p>
      ) : (
        <div className="space-y-2 mt-2">
          {tasks.map(t => (
            <Card key={t.id}>
              <CardContent className="py-3 px-4">
                <div className="flex items-center justify-between">
                  <div>
                    <span className="font-medium text-sm">{t.title}</span>
                    <Badge variant="outline" className="ml-2 text-[10px]">{t.status}</Badge>
                    <Badge variant="secondary" className="ml-1 text-[10px]">{t.priority}</Badge>
                  </div>
                  {t.due_date && (
                    <span className="text-xs text-muted-foreground">
                      {new Date(t.due_date).toLocaleDateString('nl-BE')}
                    </span>
                  )}
                </div>
              </CardContent>
            </Card>
          ))}
        </div>
      )}
    </TabsContent>

    <TabsContent value="communications">
      {communications.length === 0 ? (
        <p className="text-sm text-muted-foreground py-4">Geen communicatie gekoppeld aan deze deal.</p>
      ) : (
        <div className="space-y-2 mt-2">
          {communications.map(c => (
            <Card key={c.id}>
              <CardContent className="py-3 px-4">
                <div className="flex items-center justify-between">
                  <div>
                    <span className="font-medium text-sm">{c.subject}</span>
                    <Badge variant="outline" className="ml-2 text-[10px]">{c.type}</Badge>
                  </div>
                  <span className="text-xs text-muted-foreground">
                    {new Date(c.date).toLocaleDateString('nl-BE')}
                  </span>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>
      )}
    </TabsContent>
  </Tabs>
</div>
```

Note: DealDetail is currently NOT a client component (no 'use client'). Tabs from shadcn requires client-side interactivity. Either:
- Add `'use client'` to deal-detail.tsx (acceptable since it's already a leaf component), OR
- Extract the tabs into a separate client component `DealLinkedDataTabs` and keep deal-detail as server

Prefer adding `'use client'` to deal-detail.tsx for simplicity — it has no children that benefit from server rendering.
  </action>
  <verify>
    <automated>cd /Users/stijnkuppens/phpro/ai-projects/phpro_crm && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/deals/[id]/page.tsx contains `Promise.all`
    - src/app/admin/deals/[id]/page.tsx contains `getActivities`
    - src/app/admin/deals/[id]/page.tsx contains `getTasks`
    - src/app/admin/deals/[id]/page.tsx contains `getCommunications`
    - src/app/admin/deals/[id]/page.tsx contains `deal_id: id`
    - src/features/deals/components/deal-detail.tsx contains `Activiteiten`
    - src/features/deals/components/deal-detail.tsx contains `Taken`
    - src/features/deals/components/deal-detail.tsx contains `Communicatie`
    - src/features/deals/components/deal-detail.tsx contains `TabsTrigger`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Deal detail page shows activities, tasks, and communications tabs with deal-specific data fetched server-side via Promise.all; empty states handled; TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All three queries support deal_id filter
- Deal detail page shows 3 tabs with linked data
- Data is fetched server-side with Promise.all (no waterfalls)
</verification>

<success_criteria>
- Deal detail page shows activities tab with deal-linked activities
- Deal detail page shows tasks tab with deal-linked tasks
- Deal detail page shows communications tab with deal-linked communications
- Empty state message shown when no linked data exists
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-contacts-deals/03-05-SUMMARY.md`
</output>
